server {
    listen 80;

    # 프론트엔드 접속
    location / {
        proxy_pass http://frontend:80;
    }

    # AI 분석 API 요청 (에이전트가 쏘는 곳)
    location /api/analyze {
        proxy_pass http://ai-analyzer:8080/analyze;
    }
}